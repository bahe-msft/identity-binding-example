apiVersion: v1
kind: Namespace
metadata:
  name: demo-app-wi
  labels:
    name: demo-app-wi
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: workload-identity-sa
  namespace: demo-app-wi
  labels:
    app: demo-app-wi
  annotations:
    azure.workload.identity/client-id: "${MANAGED_IDENTITY_2_CLIENT_ID}"  # Replace with actual client ID
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app-wi
  namespace: demo-app-wi
  labels:
    app: demo-app-wi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app-wi
  template:
    metadata:
      labels:
        app: demo-app-wi
        azure.workload.identity/use: "true"  # Enable workload identity
    spec:
      serviceAccountName: workload-identity-sa
      containers:
      - name: demo-app
        image: ghcr.io/YOUR_GITHUB_USERNAME/demo-app-go:latest  # Replace with your image
        ports:
        - containerPort: 8080
        env:
        - name: KEYVAULT_URL
          value: "${KEYVAULT_URL}"  # Replace with your Key Vault URL
        - name: SECRET_NAME
          value: "demo-secret"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: SERVICE_ACCOUNT
          valueFrom:
            fieldRef:
              fieldPath: spec.serviceAccountName
        # Multiple identity environment variables - demonstrates using multiple identities in one pod
        - name: MANAGED_IDENTITY_1_CLIENT_ID
          value: "${MANAGED_IDENTITY_1_CLIENT_ID}"  # Replace with actual client ID
        - name: MANAGED_IDENTITY_2_CLIENT_ID
          value: "${MANAGED_IDENTITY_2_CLIENT_ID}"  # Replace with actual client ID
        # Workload identity specific environment variables (for Azure SDK compatibility)
        - name: AZURE_CLIENT_ID
          value: "${MANAGED_IDENTITY_2_CLIENT_ID}"  # Primary identity for workload identity
        - name: AZURE_TENANT_ID
          value: "${AZURE_TENANT_ID}"  # Replace with your tenant ID
        - name: AZURE_FEDERATED_TOKEN_FILE
          value: "/var/run/secrets/azure/tokens/azure-identity-token"
        - name: AZURE_AUTHORITY_HOST
          value: "https://login.microsoftonline.com/"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: azure-identity-token
          mountPath: /var/run/secrets/azure/tokens
          readOnly: true
      volumes:
      - name: azure-identity-token
        projected:
          defaultMode: 420
          sources:
          - serviceAccountToken:
              audience: api://AzureADTokenExchange
              expirationSeconds: 3600
              path: azure-identity-token
---
apiVersion: v1
kind: Service
metadata:
  name: demo-app-wi-service
  namespace: demo-app-wi
  labels:
    app: demo-app-wi
spec:
  selector:
    app: demo-app-wi
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP